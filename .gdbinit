# This .gdbinit is for OpenOCD (port 3333)
# For JLink debugging, Cortex-Debug will handle the connection (port 50000)
# Uncomment the lines below only if using OpenOCD with manual launch
# target extended-remote localhost:3333
# monitor reset init
# monitor halt
# load