# 待解决事项说明
更新日期：2025.1.6

1. 仿照遥控器的代码也写了拉力传感器的读取，但是两个同时开启的时候会发生遥控器和拉力传感器的示数有时候会被清0，疑似是跳到掉线的地方去了
2. 在调试机械臂时候，控制6020，在机械臂执行多次动作后会突然无法控制6020（bottom），收不到回传消息。
3. 可能由于重力影响无法快速到达指定位置，每次到达附近后还会微调。可能可以调节pid解决这个问题。
4. 拉力传感器数据目前由F_data接受，还没有写应用代码。
5. 两个3508电机的限位还没写（不知道咋写好）2006的转速还得调一下，太慢了。
6. 机械臂的代码细节还没完善


# 机械臂控制部分说明

目前所有机械臂控制代码写在了robot_cmd.c的底部

```c
void arm_task_1()；//抓取飞镖1并放置的代码封装
void arm_up_claw_open()； // 机械臂单个动作封装，机械臂抬起 夹爪打开
void arm_up_claw_close()  // 机械臂 抬起  夹爪 关闭
void arm_down_claw_open()； // 机械臂单个动作封装，机械臂放下 夹爪打开
void arm_down_claw_close()  // 机械臂放下  夹爪关闭
void arm_put_open()； // 机械臂单个动作封装，机械臂到达放置飞镖位置 夹爪打开
void arm_put_close()   // 机械臂  放置飞镖位置  夹爪关闭
```

lca
1. 尝试用掉电不丢失的模块记录下总的圈数，首先先尝试劫持初始化的代码，然后在任务里面不断写入数据，通过 软件模拟 IIC
2. 调试双板通信和串口读取拉力计的力